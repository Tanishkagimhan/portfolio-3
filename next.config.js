/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // Remove 'appDir' unless you're using a custom app directory and it's still supported
  },
  // swcMinify: true, // REMOVE this line
};

module.exports = nextConfig;
